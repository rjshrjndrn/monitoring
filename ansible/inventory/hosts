# ip where prometheus needs to run
[prometheus]

# All machine ips which needed to be monitored
[vm-node-exporter]


[all:vars]
ansible_ssh_user=someuser
ansible_ssh_private_key_file=/path/to/ssh_key

